config:
  plugin_folders:
    - 'plugins/git_workspace'
    - 'plugins/spack_workspace'


defaults:
  origin :
    url : 'https://github.com/RemoteConnectionManager/spack.git'
  upstream :
    prs : 
     - '8772'
  runconfig: 'True'
  integration: 'True'
  cache: '../../cache'

logging_configs:
  loggers:
    cascade_yaml_config:
      level: WARNING

